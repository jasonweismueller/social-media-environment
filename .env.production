# Production backend cutover (2026-08-02) — see CLAUDE.md "Backend migration"
# for the full history. Vite auto-loads this file for `npm run build`
# (production mode), no workflow changes needed. VITE_SUPABASE_ANON_KEY is
# the public/publishable key — safe to commit, every table is RLS-gated
# (see .env.example for the same note).
VITE_BACKEND=supabase
VITE_SUPABASE_URL=https://yrzqnlhbawzuzlrrocfd.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_at2FGEre9zcJy5kyw68h6g_mb2hryct

# Sentry error monitoring (2026-08-04). A DSN is a public identifier, not a
# secret — it only lets a client send events in, it grants no read/write
# access to the Sentry project. Safe to commit, same reasoning as the
# Supabase anon key above. See src/utils/utils-sentry.js for the init call.
VITE_SENTRY_DSN=https://421029bfddda6a29d580d502a1f2a76d@o4511853111345152.ingest.us.sentry.io/4511853160890368
