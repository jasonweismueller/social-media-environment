backend/.env
.env
CLAUDE.md

# Build output — regenerated by `npm run build` / the GitHub Actions deploy workflow
dist/
docs/

# Dependencies — regenerated by `npm install`
node_modules/

# Dead local-only dev tool (unused S3 upload signer) — kept on disk for local
# reference but shouldn't be tracked (includes its own node_modules).
backend/

# Supabase CLI local session cache — regenerated by `supabase` CLI commands
supabase/.temp/

# macOS Finder metadata
.DS_Store
